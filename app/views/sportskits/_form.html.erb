<%= form_with(model: sportskit, local: true) do |form| %>
  <% if sportskit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sportskit.errors.count, "error") %> prohibited this sportskit from being saved:</h2>

      <ul>
        <% sportskit.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
